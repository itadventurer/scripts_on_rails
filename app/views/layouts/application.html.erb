<!DOCTYPE html>
<html>
  <head>
    <title><%= full_title(yield :title)%></title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%= stylesheet_link_tag    "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <script src="http://d1n0x3qji82z53.cloudfront.net/src-min-noconflict/ace.js" type="text/javascript" charset="utf-8"></script>
    <link rel="icon" href="favicon.png" type="image/png" />
    <%= csrf_meta_tags %>
  </head>
  <body>

    <%= render 'layouts/header' %>
    <div class="container">
      <div class="row" style="margin-top:50px">
        <div class="breadcrumb">
          <%= render_crumbs %>
        </div>
        <% unless @nohead %>
          <h1><%= yield :title %></h1>
        <% end %>
        <%flash.each do |name, msg| %>
          <%if msg.is_a?(String) %>
            <div class="alert alert-<%= name %>">
              <a class="close" data-dismiss="alert">&#215;</a>
              <%= content_tag :div, msg, :id => "flash_#{name}" %>
            </div>
          <%end %>
        <%end %>
        <div class="span12">
          <%= yield %>
        </div>
      </div>
    <div id="footer">
    ScriptOmat is free software licensed by <a href="http://www.gnu.org/licenses/gpl.html">GPL3</a>. The sourcecode is freely available on <a href="https://github.com/azapps/scripts_on_rails">Github</a>.
    <span class="right">&copy; 2013 <a href="http://azapps.de">Anatoly Zelenin</a></span>
    </div>
    </div>

  </body>
</html>
